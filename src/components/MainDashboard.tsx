import React from 'react'

interface OptionProps {
  id?: number
  name?: string
}

const MainDashboard = (props: OptionProps) => {
  return (
    <main className='bg-gradient-to-r from-blue-800 to-blue-600 min-h-[100vh w-full'>content</main>
  )
}

export default MainDashboard
